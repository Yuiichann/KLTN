const backend_url = import.meta.env.VITE_BACKEND_URL;

const api_backend_url = import.meta.env.VITE_API_BACKEND_URL;

const main_website_url = import.meta.env.VITE_MAIN_WEBSITE_URL;

const config = {
  backend_url,
  api_backend_url,
  main_website_url,
};

export default config;
